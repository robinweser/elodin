{
  "private": true,
  "name": "elodin",
  "repository": "https://github.com/robinweser/elodin.git",
  "author": "robinweser <contact@weser.io>",
  "license": "MIT",
  "scripts": {
    "bootstrap": "lerna bootstrap --reject-cycles",
    "cleanup": "lerna run clean && lerna clean --yes",
    "build": "lerna run build",
    "format": "prettier --write \"**/*.js\"",
    "coverage": "lerna run coverage",
    "release": "git pull --rebase && yarn run cleanup && yarn build && yarn run coverage && lerna publish --force-publish",
    "setup": "yarn bootstrap && yarn build",
    "test": "lerna run test",
    "check": "yarn run test",
    "postinstall": "yarn build"
  },
  "workspaces": [
    "packages/*",
    "generators/*",
    "plugins/*",
    "examples/*",
    "runtimes/*",
    "website"
  ],
  "devDependencies": {
    "lerna": "^3.6.0",
    "prettier": "^1.15.3",
    "rimraf": "^2.6.2"
  }
}
