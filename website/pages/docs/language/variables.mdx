import DocLayout from '../../../components/DocLayout.js'

export default DocLayout

# Variables

Apart from primitive values, we can also pass variables to set dynamic properties.<br/>
Variables are resolved depending on the target.

For example, the [@elodin/generator-javascript]() will render CSS variables by default.

```elodin name=button.elo
style Button {
  backgroundColor: $bgColor
}
```

---

## Related

- [@elodin/plugin-replace-variable](/docs/plugins/replace-variable)
- [@elodin/plugin-rename-variable](/docs/plugins/rename-variable)
