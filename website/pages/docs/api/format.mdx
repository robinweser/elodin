import DocLayout from '../../../components/DocLayout.js'

export default DocLayout

# Format

```bash
yarn add --dev @elodin/format
```

Format beautifies a string of Elodin code in an opinionated approach.

## format

It exposes a single `format` method that just takes an single string of code.

### Example

```javascript
import { format } from '@elodin/format'

const input = `
style Button { backgroundColor: red
      paddingLeft: 10
paddingRight  :   10
}
`

const output = format(input)

output = `
style Button {
  backgroundColor: red
  paddingLeft: 10
  paddingRight: 10
}
`
```
